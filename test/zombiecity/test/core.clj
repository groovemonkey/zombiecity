(ns zombiecity.test.core
  (:use [zombiecity.core])
  (:use [zombiecity.userinput])
  (:use [zombiecity.mainloop])
  (:use [zombiecity.data.buildings])
  (:use [zombiecity.data.rooms])
  (:use [zombiecity.items])
  (:use [zombiecity.data.furniture])
  
  (:use [clojure.test]))

(deftest replace-me ;; FIXME: write
  (is false "No tests have been written."))
